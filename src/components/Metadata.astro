---
const { date, readtime, slug } = Astro.props;
const dateString = new Date(date).toDateString();
---
<div class="metadata bottom-spaced">
    <p>{dateString}</p>
    <p>·</p>
    <p>{`📖 ${readtime} minutes`}</p>
    {slug && (
        <div class="metadata">
            <p>·</p>
            <a target="_blank" rel="noopener noreferrer" href={`https://github.com/pol-piella/polpiella.dev/edit/main/src/_posts/${slug}.md`}>
                <b>Found a mistake? Edit on Github!</b>
            </a>
        </div>
    )}
</div>

<style>
    .metadata {
        display: flex;
        align-items: center;
        font-size: 12px;
        flex-wrap: wrap;
        line-height: 25px;
    }

    @media (prefers-color-scheme: dark) {
        .metadata > a {
            color: var(--gray100);
        }
    }

    .metadata > * {
        margin-right: 8px;
    }

    .bottom-spaced {
        margin-bottom: 8px;
    }
</style>

