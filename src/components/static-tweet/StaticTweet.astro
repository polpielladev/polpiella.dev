---
import { getTweet } from '@components/static-tweet/api'
import EmbeddedTweet from './lib/EmbeddedTweet.astro'

export interface Props {
  id: string
}

const { id } = Astro.props
const tweet = await getTweet(id)
---

<EmbeddedTweet tweet={tweet!} />
