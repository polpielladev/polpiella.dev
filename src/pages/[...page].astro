---
import BaseLayout from '@layouts/BaseLayout.astro'
import BlogPreview from '@components/BlogPreview.svelte'
import Paginator from '@components/Paginator.svelte'
import Hero from '@components/Hero.svelte'
import { getCollection } from 'astro:content'
import type { GetStaticPathsOptions } from 'astro'
import Subscribe from '@components/Subscribe.svelte'
import YoutubeVideo from '@components/YoutubeVideo.svelte'

export async function getStaticPaths({ paginate }: GetStaticPathsOptions) {
  const posts = await getCollection('blog')
  const sortedPosts = posts.sort(
    (a, b) =>
      new Date(b.data.pubDate).valueOf() - new Date(a.data.pubDate).valueOf()
  )

  return paginate(sortedPosts, {
    pageSize: 6,
  })
}

interface PageItem {
  data: PageItemData
  slug: string
}

interface PageItemData {
  title: string
  excerpt: string
  pubDate: Date
}

const { page } = Astro.props
---

<BaseLayout
  title="polpielladev 📱 | A blog about iOS mobile app development and Swift"
  description="Blog posts about iOS mobile app development and Swift by Pol Piella."
  image="https://polpiella.dev/assets/home.jpg"
  activeNav="index">
  <div class="flex flex-col gap-6">
    <section>
      <Hero
        title="Welcome to my blog 👋"
        body="I'm Pol, a content creator and iOS developer. In this blog I write about all things iOS, Swift and software development best practices. I also have a newsletter about iOS CI/CD if you want to subscribe 👇">
        <Subscribe />
      </Hero>
    </section>
    <!-- Latest -->
    <section class="flex flex-col gap-6">
      <div>
        <div class="flex justify-between">
          <h2 class="text-2xl font-title dark:text-gray-50 flex pb-1">
            Latest articles
          </h2>
          <a
            href="/posts"
            class="flex hover:underline dark:text-violet-200 dark:hover:text-violet-100 hover:text-violet-600 gap-0.5 text-violet-500 stroke-current">
            See all
            <svg
              class="w-4"
              width="24px"
              height="24px"
              stroke-width="1.5"
              viewBox="0 0 24 24"
              fill="none"
              xmlns="http://www.w3.org/2000/svg"
              color="current"
              ><path
                d="M9 6l6 6-6 6"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"></path>
            </svg>
          </a>
        </div>

        <hr />
      </div>
      <Paginator previousURL={page.url.prev} nextURL={page.url.next} />
      <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6 pt-4">
        {
          page.data.map((post: PageItem, index: number) => (
            <BlogPreview
              title={post.data.title}
              slug={post.slug}
              excerpt={post.data.excerpt}
              pubDate={post.data.pubDate}
              isNew={index == 0 && page.currentPage == 1}
            />
          ))
        }
      </div>
    </section>

    <section class="flex flex-col gap-6">
      <div>
        <h2 class="text-2xl font-title dark:text-gray-50 flex pb-1">
          Speaking
        </h2>
        <hr />
      </div>

      <div class="grid lg:grid-cols-3 sm:grid-cols-2 gap-6">
        <YoutubeVideo id="ORGwfDBfLLk" />
        <YoutubeVideo id="5Xf5ihKvogs" />
        <YoutubeVideo id="yyOXynCoagE" />
        <YoutubeVideo id="l7nN7QdiGyY" />
        <YoutubeVideo id="sZj-8sDUYAs" />
      </div>
    </section>
  </div>
</BaseLayout>
